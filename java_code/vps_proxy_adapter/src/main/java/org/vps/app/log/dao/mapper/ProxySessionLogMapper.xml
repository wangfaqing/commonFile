<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.vps.app.log.dao.mapper.ProxySessionLogMapper">

	<insert id="insertProxySessionLog" parameterType="org.vps.app.log.dao.modle.LogProxySession">
        INSERT INTO qy_vps_proxy_session_log_#{currentDate,jdbcType=INTEGER} (   
			session_id,
			ip_server,
			ip_request,
			recv_bytes,
			send_bytes,
			request_time,
			ip_header,
			ip_md5_header,
			ip_connect,
			user_type,
			pass,
			has_AUZ,
			user,
			did,
			uid,
			pid,
			cid,
			uuid,
			mark,
			ip,
			sip,
			dip,
			ipl,
			ipmuc,
			base,
			pid_u,
			cid_u,
			AUZ_src,
			url,
			connect_timestamp,
			disconnect_timestamp,
			create_time,
			input_time
        ) VALUES (
            #{sessionID},
            #{ipServer},
            #{ipRequest},
            #{recvByteCounts},
            #{sendByteCounts},
            #{requestTime},
            #{ipHeader},
            #{ipMD5Header},
            #{ipConnect},
            #{userType},
            #{pass},
            #{hasAUZ},
            #{user},
            #{did},
            #{uid},
            #{pid},
            #{cid},
            #{uuid},
            #{mark},
            #{ip},
            #{sip},
            #{dip},
            #{ipl},
            #{ipmuc},
            #{base},
            #{pid_u},
            #{cid_u},
            #{AUZInfo},
            #{url},
            #{connectTimestamp},
            #{disconnectTimestamp},
            #{createTime},
            Now()
        )
	</insert>
	
</mapper>